# -*- coding: utf-8 -*-
# vim: ft=yaml
---
driver:
  name: docker

provisioner:
  name: salt_solo
  formula: foreman
  state_top:
    base:
      "*":
        - foreman

platforms:
  - name: debian-8

suites:
  - name: default

  - name: custom
    provisioner:
      pillars-from-files:
        foreman.sls: pillar-custom.sls
      pillars:
        top.sls:
          base:
            "*":
              - foreman
